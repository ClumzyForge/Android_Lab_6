<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Android_Lab_5" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowBackground">@color/primary</item>
        <item name="android:buttonTint">@color/secondary</item>

        <!-- Splash screen
        <item name="android:windowSplashScreenAnimationDuration" tools:targetApi="33">3000</item>
        <item name="android:windowSplashScreenBackground" tools:targetApi="33">@color/red</item>
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="33">@drawable/rounded_square</item> -->
    </style>

    <style name="basicButton" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/secondary</item>
        <item name="backgroundColor">@color/secondary</item>
        <item name="android:textColor">@color/black</item>
        <item name="fontFamily">serif-monospace</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">15sp</item>
    </style>

    <style name="verLayout" parent="ThemeOverlay.Material3">
    </style>

    <style name="editTextInput" parent="Widget.AppCompat.EditText">
        <item name="android:gravity">center</item>
        <item name="android:inputType">number</item>
        <item name="android:padding">10dp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Theme.Android_Lab_5" parent="Base.Theme.Android_Lab_5" />
</resources>